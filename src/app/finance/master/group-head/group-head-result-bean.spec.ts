import {GroupHeadResultBean } from './group-head-result-bean';

describe('SubGroupAccountResultBean', () => {
  it('should create an instance', () => {
    expect(new GroupHeadResultBean()).toBeTruthy();
  });
});
