import {AccountHeadMasterResultBean } from './account-head-result-bean';

describe('AccountHeadMasterResultBean', () => {
  it('should create an instance', () => {
    expect(new AccountHeadMasterResultBean()).toBeTruthy();
  });
});
